# CASTLE: Context-Aware Semantic Transformer with Knowledge Graph Enhancement
# Requirements for Indonesian GEC

# Core deep learning
torch>=2.0.0
torchvision>=0.15.0

# Fairseq (for Transformer seq2seq backbone)
fairseq==0.10.2

# HuggingFace (dataset loading + tokenizer)
datasets>=2.14.0
transformers>=4.35.0
tokenizers>=0.14.0
huggingface_hub>=0.19.0

# Indonesian NLP tools
PySastrawi==1.0.0          # Sastrawi stemmer for morphological analysis

# Knowledge Graph utilities
networkx>=3.1               # Graph construction and traversal
scipy>=1.11.0               # Statistical collocation (PMI)

# Evaluation
sacrebleu>=2.3.1            # BLEU score
nltk>=3.8.1                 # NLTK for tokenization in eval
errant>=2.3.3               # M2 scorer for F1 evaluation (optional)

# Training utilities
accelerate>=0.24.0          # FP16 / mixed precision
tensorboard>=2.14.0         # Logging

# DeepSeek API (for neural KG construction)
openai>=1.3.0               # DeepSeek uses OpenAI-compatible API

# Data processing
pandas>=2.0.0
numpy>=1.24.0
tqdm>=4.66.0
python-Levenshtein>=0.23.0  # String similarity for KG validation

# Utilities
pyyaml>=6.0.1
click>=8.1.7
